spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
app:
  upload-dir: ${UPLOAD_DIR:/data/uploads}
jwt:
  secret: ${JWT_SECRET}